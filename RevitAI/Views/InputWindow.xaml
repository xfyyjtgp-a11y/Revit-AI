<Window x:Class="RevitAI.Views.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revit AI Assistant" Height="250" Width="450" WindowStartupLocation="CenterScreen" Topmost="True">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="请输入建模指令：" FontWeight="Bold" Margin="0,0,0,5"/>
        
        <TextBox x:Name="InputTextBox" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"
                 Padding="5"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>
        
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Text="{Binding StatusText}" VerticalAlignment="Center" Foreground="Gray"/>
            <Button x:Name="GenerateButton" Grid.Column="1" Content="生成 (Generate)" Command="{Binding GenerateCommand}" Padding="15,5" Height="30"/>
        </Grid>
    </Grid>
</Window>

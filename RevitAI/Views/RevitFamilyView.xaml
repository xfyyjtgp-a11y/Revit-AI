<Window x:Class="RevitAI.Views.RevitFamilyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm= "clr-namespace:RevitAI.ViewModels"
        xmlns:model="clr-namespace:RevitAI.Models"
        mc:Ignorable="d"
        Title="Revit族信息浏览器" Height="450" Width="400">
    <!--<Window.DataContext>
        <vm:RevitFamilyViewModel />
    </Window.DataContext>-->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Content="打开 Revit 文件" Command="{Binding OpenFileCommand}" Margin="0,0,0,10" Padding="10,5"/>

        <GroupBox Grid.Row="1" Header="Revit 族列表">
            <TreeView ItemsSource="{Binding Families}" Margin="5"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type model:RevitFamily}" ItemsSource="{Binding Types}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📦 " />
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                    
                    <DataTemplate DataType="{x:Type model:RevitFamilyType}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔹 " />
                            <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>
        </GroupBox>
    </Grid>
</Window>
